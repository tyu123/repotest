#include <iostream>

using namespace std;

int main(int argc, char **argv)
{
    int a,b,c;
    a = b = c = 0;
    cout << "podaj 3 liczby: ";
    cin >> a >> b >> c;
    if (a > b && a >c>)
    {
     cout << "<Największe a=" << a;
    } else if (a > b && a > c)
}
    {
        if (b > a && b > c)
    }
    {   if (c > a && c > b)
    }
    if (a == b && a > c)
    {
     cout << "Największe: a = b = " <<a;
     
     if (a == c && a > b)
    {
     cout << "Największe: a = c = " <<a;
     
     if (b == c && b > a)
    {
     cout << "Największe: b = c = " <<b;
     
     
     
    cout << b;
    } else //if (a == b)
    
    {
    cout << "Równe, a=" << a <<", b=" << b;
    } 
    return 0;
